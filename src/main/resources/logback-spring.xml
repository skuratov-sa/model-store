<configuration>
    <springProperty scope="context" name="model-store-backend" source="spring.application.name"/>

    <!-- ================= Консоль ================= -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%X{traceId}] [%thread]  %logger{36} -  - %msg%n%ex{full} - </pattern>
        </encoder>
    </appender>

    <!-- ================= Файл ================= -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>/opt/logs/model-store-backend.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Новый файл каждый день -->
            <fileNamePattern>/opt/logs/model-store-backend.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- Храним только 14 дней -->
            <maxHistory>14</maxHistory>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp/>
                <pattern>
                    <pattern>
                        {"level": "%level", "logger": "%logger", "message": "%message", "thread": "%thread", "traceId": "%X{traceId}"}
                    </pattern>
                </pattern>
                <mdc/>
                <arguments/>
                <message/>
                <throwable/> <!-- добавит поля: exception и stacktrace -->
            </providers>
        </encoder>
    </appender>

    <!-- ================= Root ================= -->
    <root level="DEBUG">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>
</configuration>
